language: python

services:
  - docker

before_install:
- docker build -t nets .

script:
- docker run --env LOCAL_USER_ID=$(id -u $USER) --volume $(pwd):/opt/data -p 127.0.0.1:8080:8080 nets /bin/bash -c "pip install -r requirements.txt && make test"